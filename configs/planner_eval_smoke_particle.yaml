experiment:
  name: "smoke_particle"
  shared_plan:
    imports:
      - task/pusht_smoke_dataset.yaml
      - planner/smoke.yaml
    plan:
      artifacts:
        render: false
        save: false
  rollouts:
    seed: 5
    num_rollouts: 2
    sample_without_replacement: true
  execution:
    mode: "process"
    max_workers: 2
  reporting:
    output_root: "rollouts"
  baseline: "finest_fixed"
  variants:
    - name: "finest_fixed"
      imports:
        - backend/particle_state.yaml
        - fidelity/finest.yaml
    - name: "coarsest_fixed"
      imports:
        - backend/particle_state.yaml
        - fidelity/coarsest.yaml
