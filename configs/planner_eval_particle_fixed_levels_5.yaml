experiment:
  name: "particle_fixed_levels_5"
  shared_plan:
    imports:
      - task/pusht_dataset.yaml
      - planner/default.yaml
      - backend/particle_state.yaml
    plan:
      artifacts:
        render: false
        save: false
  rollouts:
    seed: 0
    num_rollouts: 5
    sample_without_replacement: true
  execution:
    mode: "auto"
    max_workers: 6
  reporting:
    output_root: "rollouts"
  baseline: "particle_l7_n252"
  variants:
    - name: "particle_l0_n1"
      overrides:
        planner:
          fidelity:
            enabled: true
            mpc:
              mode: "fixed"
              level: 0
            cem:
              mode: "fixed"
              level: "base"
            rollout:
              mode: "fixed"
              level: "base"
    - name: "particle_l1_n2"
      overrides:
        planner:
          fidelity:
            enabled: true
            mpc:
              mode: "fixed"
              level: 1
            cem:
              mode: "fixed"
              level: "base"
            rollout:
              mode: "fixed"
              level: "base"
    - name: "particle_l2_n3"
      overrides:
        planner:
          fidelity:
            enabled: true
            mpc:
              mode: "fixed"
              level: 2
            cem:
              mode: "fixed"
              level: "base"
            rollout:
              mode: "fixed"
              level: "base"
    - name: "particle_l3_n4"
      overrides:
        planner:
          fidelity:
            enabled: true
            mpc:
              mode: "fixed"
              level: 3
            cem:
              mode: "fixed"
              level: "base"
            rollout:
              mode: "fixed"
              level: "base"
    - name: "particle_l4_n16"
      overrides:
        planner:
          fidelity:
            enabled: true
            mpc:
              mode: "fixed"
              level: 4
            cem:
              mode: "fixed"
              level: "base"
            rollout:
              mode: "fixed"
              level: "base"
    - name: "particle_l5_n64"
      overrides:
        planner:
          fidelity:
            enabled: true
            mpc:
              mode: "fixed"
              level: 5
            cem:
              mode: "fixed"
              level: "base"
            rollout:
              mode: "fixed"
              level: "base"
    - name: "particle_l6_n128"
      overrides:
        planner:
          fidelity:
            enabled: true
            mpc:
              mode: "fixed"
              level: 6
            cem:
              mode: "fixed"
              level: "base"
            rollout:
              mode: "fixed"
              level: "base"
    - name: "particle_l7_n252"
      overrides:
        planner:
          fidelity:
            enabled: true
            mpc:
              mode: "fixed"
              level: 7
            cem:
              mode: "fixed"
              level: "base"
            rollout:
              mode: "fixed"
              level: "base"
