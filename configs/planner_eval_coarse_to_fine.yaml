experiment:
  name: "wm_coarse_to_fine_100_rollouts"
  shared_plan:
    imports:
      - task/pusht_dataset.yaml
      - planner/default.yaml
    plan:
      artifacts:
        render: false
        save: false
  rollouts:
    seed: 0
    num_rollouts: 20
    sample_without_replacement: true
  execution:
    mode: "auto"
    max_workers: 2
  reporting:
    output_root: "rollouts"
  baseline: "wm_coarse_to_fine"
  variants:
    - name: "wm_coarse_to_fine"
      imports:
        - backend/wm_coarse_to_fine.yaml
        - fidelity/coarse_to_fine.yaml
