experiment:
  name: "wm_64_100_rollouts"
  shared_plan:
    imports:
      - task/pusht_dataset.yaml
      - planner/default.yaml
    plan:
      artifacts:
        render: false
        save: false
  rollouts:
    seed: 0
    num_rollouts: 100
    sample_without_replacement: true
  execution:
    mode: "auto"
    max_workers: 2
  reporting:
    output_root: "rollouts"
  baseline: "wm_64"
  variants:
    - name: "wm_64"
      imports:
        - backend/wm_64.yaml
        - fidelity/single.yaml
